 """/* Azure CI/CD with Docker & Jenkins — Stylesheet */
:root {
  --bg: #0b1020;
  --bg-soft: #121938;
  --card: #0f1530;
  --text: #e9ecf8;
  --muted: #a9b1d6;
  --brand: #4f8cff;
  --accent: #7aa2ff;
  --ok: #38d39f;
  --warn: #ffcc66;
  --border: rgba(255,255,255,0.08);
  --radius: 16px;
  --shadow: 0 10px 30px rgba(0,0,0,0.3);
}

@media (prefers-color-scheme: light) {
  :root {
    --bg: #f5f7ff;
    --bg-soft: #eef2ff;
    --card: #ffffff;
    --text: #0d1220;
    --muted: #4a5770;
    --brand: #2f6bff;
    --accent: #4f8cff;
    --ok: #1f9e77;
    --warn: #b58900;
    --border: rgba(0,0,0,0.08);
  }
}

* { box-sizing: border-box; }
html, body { height: 100%; }
body {
  margin: 0;
  font: 16px/1.6 system-ui, -apple-system, Segoe UI, Roboto, Ubuntu, Cantarell, Noto Sans, Arial, "Apple Color Emoji", "Segoe UI Emoji";
  color: var(--text);
  background: radial-gradient(1200px 600px at 10% -10%, rgba(79,140,255,0.18), transparent 60%),
              radial-gradient(900px 500px at 100% 0%, rgba(122,162,255,0.14), transparent 60%),
              var(--bg);
}

.container {
  width: min(1100px, 92vw);
  margin-inline: auto;
}

.site-header {
  position: sticky;
  top: 0;
  z-index: 10;
  backdrop-filter: blur(8px);
  background: linear-gradient(to bottom, rgba(15,21,48,0.85), rgba(15,21,48,0.65));
  border-bottom: 1px solid var(--border);
}
@media (prefers-color-scheme: light) {
  .site-header { background: linear-gradient(to bottom, rgba(255,255,255,0.85), rgba(255,255,255,0.65)); }
}
.header-inner {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 14px 8px;
}
.logo {
  display: inline-flex;
  align-items: center;
  gap: 10px;
  font-weight: 700;
  letter-spacing: .2px;
}
.logo-dot {
  width: 14px; height: 14px; border-radius: 50%;
  background: radial-gradient(circle at 30% 30%, var(--accent), var(--brand));
  box-shadow: 0 0 18px rgba(79,140,255,0.8);
}
.nav a {
  color: var(--text);
  text-decoration: none;
  margin-left: 16px;
  font-weight: 600;
  opacity: .85;
}
.nav a:hover { opacity: 1; text-decoration: underline; }

.hero {
  padding: 64px 0 36px;
  text-align: center;
}
.hero h1 {
  margin: 0 0 10px;
  font-size: clamp(28px, 4.2vw, 46px);
  line-height: 1.15;
}
.subtitle {
  margin: 0 auto 16px;
  color: var(--muted);
  max-width: 720px;
}
.badges { list-style: none; padding: 0; margin: 18px 0 0; display: flex; flex-wrap: wrap; gap: 10px; justify-content: center; }
.badges li {
  border: 1px solid var(--border);
  border-radius: 999px;
  padding: 8px 12px;
  background: linear-gradient(to bottom right, rgba(255,255,255,0.02), rgba(255,255,255,0));
}

.card {
  background: linear-gradient(180deg, rgba(255,255,255,0.02), rgba(255,255,255,0.0)), var(--card);
  border: 1px solid var(--border);
  border-radius: var(--radius);
  padding: clamp(16px, 2.8vw, 28px);
  box-shadow: var(--shadow);
  margin: 20px auto;
}

.grid.two {
  display: grid;
  grid-template-columns: 1fr;
  gap: 16px;
}
@media (min-width: 820px) {
  .grid.two { grid-template-columns: 1fr 1fr; gap: 24px; }
}

h2 {
  margin: 6px 0 12px;
  font-size: clamp(22px, 3.5vw, 32px);
}
h3 { margin: 12px 0 8px; font-size: 18px; }

.checklist {
  list-style: none; padding: 0; margin: 0;
}
.checklist li {
  position: relative;
  padding-left: 26px;
  margin: 8px 0;
}
.checklist li::before {
  content: "✓";
  position: absolute;
  left: 0; top: 0;
  color: var(--ok);
  font-weight: 800;
}

.arch {
  display: grid;
  grid-template-columns: 1fr auto 1fr auto 1fr auto 1fr;
  align-items: center;
  gap: 8px;
  overflow-x: auto;
  padding-bottom: 8px;
}
.arch-node {
  padding: 14px 12px;
  border: 1px dashed var(--border);
  border-radius: 12px;
  text-align: center;
  font-weight: 700;
  background: linear-gradient(180deg, rgba(255,255,255,0.04), rgba(255,255,255,0.01));
}
.arch-small { display: block; font-weight: 500; color: var(--muted); font-size: 12px; margin-top: 4px; }
.arch-arrow {
  white-space: nowrap;
  font-size: 12px;
  color: var(--muted);
}
.arch .cloud   { border-color: rgba(122,162,255,0.6); }
.arch .jenkins { border-color: rgba(255,204,102,0.7); }
.arch .registry{ border-color: rgba(79,140,255,0.7); }
.arch .app     { border-color: rgba(56,211,159,0.7); }

.steps {
  counter-reset: step;
  list-style: none;
  padding: 0;
  margin: 0;
}
.steps li {
  counter-increment: step;
  margin: 12px 0;
  padding-left: 44px;
  position: relative;
}
.steps li::before {
  content: counter(step);
  position: absolute;
  left: 0; top: 0;
  width: 30px; height: 30px;
  border-radius: 8px;
  display: grid; place-items: center;
  border: 1px solid var(--border);
  background: linear-gradient(180deg, rgba(255,255,255,0.07), rgba(255,255,255,0.02));
  font-weight: 800;
}

pre {
  overflow-x: auto;
  background: rgba(0,0,0,0.35);
  border: 1px solid var(--border);
  border-radius: 12px;
  padding: 14px;
}
@media (prefers-color-scheme: light) {
  pre { background: #f6f8ff; }
}
code { font-family: ui-monospace, SFMono-Regular, Menlo, Consolas, "Liberation Mono", monospace; }

.note {
  color: var(--muted);
  margin-top: 8px;
}

.tips {
  padding-left: 18px;
}
.tips li { margin: 8px 0; }

.site-footer {
  padding: 40px 0 60px;
  text-align: center;
  color: var(--muted);
}
"""

# Write files
html_path = "/mnt/data/azure-docker-jenkins-cicd.html"
css_path = "/mnt/data/azure-docker-jenkins-cicd.css"

with open(html_path, "w", encoding="utf-8") as f:
    f.write(html_content)

with open(css_path, "w", encoding="utf-8") as f:
    f.write(css_content)

html_path, css_path
